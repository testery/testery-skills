---
description: Follow a Testery test run until completion and report the final status.
---

Use the `testery-monitor-test-run` skill (`testery monitor-test-run`).

User input: $ARGUMENTS

If a test run ID is not provided, ask for one (or run `testery list-active-test-runs` to choose).
