---
description: Create a Testery alert (notify on test run failure / threshold).
---

Use the `testery-create-alert` skill. The CLI does not currently expose this directly: offer the user the web UI route or a direct API call.

User input: $ARGUMENTS
