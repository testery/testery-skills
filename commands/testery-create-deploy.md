---
description: Notify Testery of a deploy (triggers any ON_DEPLOY schedules).
---

Use the `testery-create-deploy` skill (`testery create-deploy`). Required: `--project`, `--environment`.

User input: $ARGUMENTS
