---
description: Attach a file (artifact, log, input) to an existing Testery test run.
---

Use the `testery-add-file` skill (`testery add-file`). Required: `--test-run-id`, `--file-path`, `--kind`.

User input: $ARGUMENTS
