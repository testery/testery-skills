---
description: Upload a file and attach it to a Testery environment.
---

Use the `testery-upload-environment-file` skill (`testery upload-environment-file`). Required: `--environment-key`, `--file-name`, `--source-path`.

User input: $ARGUMENTS
