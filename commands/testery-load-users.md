---
description: Bulk-load users into a Testery account from a JSON/CSV file.
---

Use the `testery-load-users` skill (`testery load-users`). Required: `--user-file`. Confirm format with the user before running.

User input: $ARGUMENTS
