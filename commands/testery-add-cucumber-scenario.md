---
description: Add a new Scenario (or Feature) to a CucumberJS + Playwright project, scaffolding step definitions as needed.
---

Use the `cucumber-add-scenario` skill. If new steps are introduced, follow up with `cucumber-add-step-definition` (and `cucumber-add-page-object` if a new page is involved).

User input: $ARGUMENTS
