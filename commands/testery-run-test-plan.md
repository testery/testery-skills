---
description: Execute a saved Testery test plan against an environment.
---

Use the `testery-run-test-plan` skill (`testery run-test-plan`). Required: `--test-plan-key`, `--environment-key`.

User input: $ARGUMENTS
