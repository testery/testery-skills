---
description: Update an existing Testery environment (rename, change pipeline stage, set variables).
---

Use the `testery-update-environment` skill (`testery update-environment`).

User input: $ARGUMENTS
